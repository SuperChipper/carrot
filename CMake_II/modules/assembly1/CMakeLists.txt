aux_source_directory(src AssSrc1)
add_library(assembly1 ${AssSrc1})
target_include_directories(assembly1 PUBLIC include)
if(BUILD_TESTS)
aux_source_directory(test AssTest1)
add_executable(ass_test ${AssTest1})
target_link_libraries(ass_test assembly1)
add_test(assembly1_test ass_test)
endif()
